apiVersion: value
kind: Service
metadata:
  name: messaging-app-Service
spec:
  type: ClusterIP
  selector:
    app: messaging-app
ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000